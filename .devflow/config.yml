# devflow configuration
# Generated by devflow init — edit as needed

platform: github-actions

stack:
  - language: python
    path: .
  - language: javascript
    path: .
  - language: python
    path: hooks/

severity:
  cve_block: [critical, high]
  cve_warn: [medium]

